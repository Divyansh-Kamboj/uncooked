{
  "name": "uncooked",
  "version": "1.0.0",
  "description": "A FastAPI application for processing exam papers",
  "main": "main.py",
  "scripts": {
    "start": "uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "dev": "uvicorn main:app --reload"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.52.0"
  }
}
